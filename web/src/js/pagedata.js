/**
 * pagedata.js — ชื่อไฟล์ตายตัวตามสัญญาระหว่างโมดูล §E.1 (export: getPageData)
 * เนื้อจริง implement ไว้ใน components.js — ดูหมายเหตุใน tokens.js
 */
export { getPageData } from './components.js';
