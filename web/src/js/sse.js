/**
 * sse.js — ชื่อไฟล์ตายตัวตามสัญญาระหว่างโมดูล §E.1 (export: streamSSE, และ AskError ที่ streamSSE โยน)
 * เนื้อจริง implement ไว้ใน components.js — ดูหมายเหตุใน tokens.js
 */
export { streamSSE, AskError } from './components.js';
