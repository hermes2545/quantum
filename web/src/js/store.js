/**
 * store.js — ชื่อไฟล์ตายตัวตามสัญญาระหว่างโมดูล §E.1 (export: readJSON, writeJSON, PROGRESS_KEY, turnsKey)
 * เนื้อจริง implement ไว้ใน components.js — ดูหมายเหตุใน tokens.js
 */
export { readJSON, writeJSON, PROGRESS_KEY, turnsKey } from './components.js';
